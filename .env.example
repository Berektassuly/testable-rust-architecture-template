# =============================================================================
# Testable Rust Architecture Template - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment.
# DO NOT commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app_dev

# -----------------------------------------------------------------------------
# Blockchain Configuration
# -----------------------------------------------------------------------------
# Solana RPC endpoint URL
# Options:
#   - Devnet: https://api.devnet.solana.com
#   - Testnet: https://api.testnet.solana.com
#   - Mainnet: https://api.mainnet-beta.solana.com
SOLANA_RPC_URL=https://api.devnet.solana.com

# Base58-encoded Ed25519 private key for signing transactions
# Generate with: solana-keygen new --no-bip39-passphrase
# WARNING: Keep this secret! Never commit real keys to version control.
ISSUER_PRIVATE_KEY=YOUR_BASE58_ENCODED_PRIVATE_KEY_HERE

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Host to bind the server to
HOST=0.0.0.0

# Port to listen on
PORT=3000

# Enable rate limiting (recommended for production)
# Values: true, false, 1, 0
ENABLE_RATE_LIMITING=false

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level filter
# Format: target=level,target2=level2
# Levels: trace, debug, info, warn, error
# Examples:
#   - RUST_LOG=info
#   - RUST_LOG=debug,tower_http=trace
#   - RUST_LOG=testable_rust_architecture_template=debug,sqlx=warn
RUST_LOG=info,tower_http=debug,sqlx=warn

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Enable backtrace for debugging (development only)
RUST_BACKTRACE=1
